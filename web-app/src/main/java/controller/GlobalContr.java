package controller;

public class GlobalContr {
    /**
     * Если этот параметр установлен как true, тогда детальные сообщения об 
     * ошибках будут отображаться в браузере, иначе только в лог файле.
     * 
     * If this parameter is true, then detail description of exceptions will
     * be shown in browser, otherwise only in log file.
     */
    public static final boolean DEBUG = false;
}